<div class="main-container">
  <div class="title">
    <h1>My Closet <i class="fas fa-seedling"></i>
    </h1>
    <h2>You are <b><%=@average.round(1)%>%</b> eco-friendly.</h2>
    <p><%= link_to "Add another item", new_garment_path, class: "btn btn-primary mt-3" %></p>
  </div>

  <div class="container" style="background-image: url('<%= cl_image_path("https://images.unsplash.com/photo-1495195129352-aeb325a55b65?ixlib=rb-1.2.1&auto=format&fit=crop&w=1055&q=80") %>')">
    <div class="cards">
      <% @purchases.each do |purchase| %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.1), rgba(0,0,0,0.1)), url('<%= cl_image_path(purchase.garment.image.key, width: 300, height: 400, crop: :fill) if purchase.garment.image.attached? %>')">
        <div class="card-text">
          <div class="card-title">
            <h3>
              <%= purchase.garment.name %>
            </h3>
          </div>
          <div class="card-info-1">
            <p> Eco-Rating: <%=purchase.garment.percentage%>%</p>
          </div>
          <div class="card-info-2">
            <p> Brand: <%= purchase.garment.brand.name %> </p>
          </div>
          <div class="btn-delete">
            <%= link_to purchase_path(purchase),
            method: :delete,
            data: { confirm: "Delete this purchased item?" } do %>
            <i class="fas fa-trash"></i>
            <% end %>
          </div>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
